"""
Perform backpropagation for model building in fuzzy-style logics.

Variables are fixed dimensional vectors.
"""

# Bill runs
# exists(x, bill(x) and run(x))

class Max(object):
    def __init__(self, parents):
        self.parents = parents

    def forward(inputs):
        return
